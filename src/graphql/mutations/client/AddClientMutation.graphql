mutation AddClientMutation($input: CreateClientInput!) {
    createClient(input: $input) {
        client {
            id
            name
            address
            zipCode
            createdAt
            updatedAt
        }
    }
}