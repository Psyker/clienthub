mutation EditClientMutation($input: EditClientInput!) {
    editClient(input: $input) {
        client {
            id
            name
            address
            zipCode
            businessSegment
            city
            description
            createdAt
            updatedAt
            phone
        }
    }
}
